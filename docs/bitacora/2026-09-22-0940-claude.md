# Nace `studio-rd`: repositorio independiente de STUDIO (copia del POS de NEXUS PRO)

**Fecha:** 2026-09-22 · **Autor:** Claude · **Decisión del dueño:** "Tenemos que cambiar todo porque STUDIO no tiene
un repositorio independiente" → base = copia del POS de NEXUS PRO, deploy = Cloudflare Worker propio, nombre `studio-rd`.

## Contenido

`git archive` de `sterlinr08-dte/nexus-pro` en el commit `6328e66` (rama `claude/review-claude-md-jpzk7k`, que
incluye `main` 58.84 + login STUDIO), quitando: `docs/` de NEXUS, `blueprints/`, maquetas `muestra-*.html`,
`supabase/functions` y `supabase/migrations` (Seguros), `.github`, herramientas locales de Claude, documentos de
reglas propios de NEXUS y `scripts/` de pruebas. Se conserva **todo el runtime** (`index.html`, los 72 `parches-*`,
iconos, logos, `sw.js`, `worker.js`, `wrangler.jsonc`) para que la app funcione idéntica desde el primer día.

## Cambios respecto a NEXUS PRO

| Archivo | Cambio |
|---|---|
| `index.html` | Base Supabase fija STUDIO (`edbknlkjnlfmkkiizdbe`), sin mapa por dominio; piel `nx-studio` siempre; título y nombres PWA "Studio"; `version.json` de respaldo apunta a este repo. |
| `worker.js` | Logo de Open Graph → `studiord.net/logo-studio.png`. |
| `wrangler.jsonc` | Worker `studio-rd`. |
| `manifest.json` | Nombre, descripción y colores de STUDIO (iconos siguen siendo los de NEXUS: pendiente). |
| `sw.js` | Caché `studio-rd-v1`. |
| `version.json` | URL `studiord.net`. |
| `parches-pos.js` | Texto de ayuda de usuarios: `studiord.net`. |
| Nuevos | `CLAUDE.md`, `README.md`, `docs/bitacora/`. |

## Pendiente para que esté en vivo

1. Dueño: crear el repo vacío `sterlinr08-dte/studio-rd` (privado) en GitHub → Claude sube este contenido.
2. Dueño: Cloudflare → Workers & Pages → Create → **Import a repository** → `studio-rd`, rama `main`, sin build.
3. Claude: mover los dominios `studiord.net` y `www.studiord.net` del Worker `nexus-pro` al Worker `studio-rd`
   (API) y quitar de NEXUS PRO el mapa por dominio y la piel STUDIO (ya no hacen falta allí).
