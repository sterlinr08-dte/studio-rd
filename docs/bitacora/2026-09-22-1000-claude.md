# `studio-rd` en vivo: repo subido, Worker `studio-rd` creado y dominios `studiord.net` asignados

**Fecha:** 2026-09-22 · **Autor:** Claude · **Referencia:** `2026-09-22-0940-claude.md` (nacimiento del repo).
**Rama:** `claude/bitacora-despliegue` (solo documentación; se mergea a `main` cuando el dueño lo autorice).

## Hecho

1. **GitHub.** El dueño creó `sterlinr08-dte/studio-rd` (público, con "Initial commit" de README). Claude rebasó el
   contenido preparado encima y subió `main` = `16287f9` (141 archivos). El README de GitHub se reemplazó por el
   README completo del proyecto.
2. **Cloudflare Worker.** El dueño creó el Worker `studio-rd` con "Import a repository" (rama `main`, comando de
   compilación vacío, deploy `npx wrangler deploy`, sin Cloudflare Access). Primer deploy 04:03 UTC. Bindings: solo
   `ASSETS`. `https://studio-rd.sterlinr08.workers.dev/` sirve `index.html` con título "Studio — Hogar, movilidad
   y tecnología", base fija `edbknlkjnlfmkkiizdbe` y sin referencia a la base madre.
3. **Dominios.** Claude movió `studiord.net` y `www.studiord.net` del Worker `nexus-pro` al Worker `studio-rd` con
   `PUT /accounts/…/workers/domains` + `override_existing_origin:true` (mismos ids de dominio y certificados) y purgó
   la caché de la zona `cb0458a5fa1f37df5a56cb32903724af` (purga confirmada). `https://studiord.net/` y
   `/version.json` (58.84) responden desde el Worker nuevo; `https://nexusprord.com/` no cambió.
4. **NEXUS PRO.** En `nexus-pro` (rama `claude/review-claude-md-jpzk7k`, commit `f786436`, sin publicar) se quitó el
   mapa de bases por dominio y se revirtió la piel de login STUDIO; ver
   `nexus-pro/docs/bitacora/2026-09-22-1000-claude.md`.

## Cómo se publica desde ahora

Cada push a `main` de este repositorio redespliega el Worker `studio-rd` automáticamente. Regla vigente: no
mergear/pushear a `main` sin autorización explícita del dueño; subir `APP_VERSION` y `version.json` en cada release.

## Pendientes

- Rotar la Global API Key de Cloudflare (el dueño) y actualizar el Vault de la base madre.
- Bloques B (firma por link), D (WhatsApp) y E (reportes) de Financiamiento con ChatGPT; documentos; PWA propia
  (manifest/iconos aún de NEXUS); retirar módulos de Seguros del runtime.
