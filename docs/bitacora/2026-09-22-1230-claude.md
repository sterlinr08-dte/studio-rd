# Ruta fija de lectura para ChatGPT y Claude: `AGENTS.md`

**Fecha:** 2026-09-22 · **Autor:** Claude · **Publicado en `main`** (solo documentación; el dueño pidió "fija una ruta para que ChatGPT siempre vaya y lea").

- Nuevo `AGENTS.md` en la raíz: orden de lectura obligatorio (AGENTS → CLAUDE → bitácora README → 5 últimas
  bitácoras → DESIGN.md → código), dónde escribir, reglas no negociables, **estado vivo** que cada entrega actualiza,
  y coordinación entre las dos IA (ramas `chatgpt/*`/`codex/*` y `claude/*`, un PR por entrega con su bitácora).
- Codex lee `AGENTS.md` automáticamente; ChatGPT debe recibir la URL fija
  `https://github.com/sterlinr08-dte/studio-rd/blob/main/AGENTS.md` al inicio de cada conversación.
- `CLAUDE.md` y `docs/bitacora/README.md` apuntan ahora a `AGENTS.md`.
