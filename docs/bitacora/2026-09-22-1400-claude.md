# 2026-09-22 14:00 — Claude — Relieve visual STUDIO (58.90): «que no quede plano o no se vea»

## Solicitud del dueño
Tras confirmar el fix de la barra lateral: «Excelente, ahora trabaja para que no quede plano o no se vea».
Interpretación: la línea gráfica 58.88 (oro plano, tarjetas sin sombra) se ve demasiado plana y algunos
elementos (cajas de iconos, bordes, textos secundarios) casi no se distinguen.

## Diagnóstico (capturas 1280 y 390 con Supabase simulado)
- Tarjetas KPI y del lanzador con sombra de 4 %: se fundían con el lienzo.
- Cajas de iconos del lanzador y de las filas de producto con el mismo color del lienzo: invisibles.
- Botones oro planos sin sombra; menú lateral negro plano.
- Textos secundarios (`--studio-mute` #9a968c) y bordes (14 %) demasiado tenues.

## Cambios (solo CSS; sin lógica, Supabase, pagos ni WhatsApp)
- `studio-brand-theme.css`
  - Tokens: lienzo `#F3F0E8`, `--studio-steel` `#5B5951`, `--studio-mute` `#85817A`, líneas 18 %/16 %,
    `--shadow-sm` en dos capas (contacto + ambiente 14 %), `--shadow-md` más profunda.
  - Nueva sección «STUDIO relieve»: superficies con brillo superior y sombra; hover con `--shadow-md`;
    iconos con caja degradada, borde y brillo (oro al pasar sobre un app); línea dorada de 2 px arriba de
    cada KPI; oro con degradado suave `#DCB63E→#C9A227→#B8921F`, brillo, sombra dorada, hover/active;
    botones secundarios y chips con sombra corta; campos con hueco interior; buscadores compuestos con el
    hueco en la píldora (el campo interior queda transparente); filas de producto con separador y hover;
    cabecera móvil con sombra; barra de Factura con más presencia; `prefers-contrast`/`reduced-transparency`.
- `studio-sidebar-dock.css`: barra con degradado vertical `#1D1D1D→#0A0A0A` y brillos internos; cajas de
  iconos con degradado, borde y sombra; ítem activo con degradado dorado, borde y resplandor; logo y
  caja del ítem activo con oro degradado.
- `DESIGN.md` §14: documenta la decisión (sustituye el «oro plano / sin degradados» de §12.7, §12.8 y §13).
- `index.html` `APP_VERSION` 58.89 → 58.90; `version.json` entrada 58.90.

## Pruebas
Chromium offline (Supabase simulado): Inicio, Vender y Factura en 1280 y 390; menú móvil abierto.
Sin errores de consola ni 404. Capturas: `rel-escritorio-*.png`, `rel-movil-*.png` (enviadas al dueño).

## Estado
Rama `claude/fix-dock-breakpoint-860` (incluye 58.89 fix barra + 58.90 relieve). **Sin publicar.**

## Siguiente paso
Dueño revisa las capturas; si aprueba, «publícalo» lleva ambas entregas a `main`. Pendiente QA en iPhone real.
