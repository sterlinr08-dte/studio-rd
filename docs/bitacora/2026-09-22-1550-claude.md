# 2026-09-22 15:50 — Claude — Publicación 58.91 en main (studiord.net)

## Autorización
Dueño: «Publícalo» tras ver el fix del menú lateral en iPhone.

## Qué se publicó (fast-forward de `claude/fix-dock-breakpoint-860` → `main`, commit `8be3f2a`)
- 58.89 · barra lateral entre 769 y 860 px (corte único de 860 px) — bitácora 1330.
- 58.90 · relieve visual (sombras, iconos con caja, oro con degradado, dock con profundidad) — bitácora 1400.
- 58.91 · menú lateral en iPhone/Safari: altura explícita y lista que no colapsa — bitácora 1540.
- `supabase/studio/16_migracion_legacy.sql` (script de la migración de datos ya ejecutada) — bitácoras 1430 y 1500.

## Deploy
Integración Git de Cloudflare Workers (`studio-rd`). Verificación programada: `version.json` = 58.91 y
`studio-sidebar-dock.css` con `height: calc(100dvh - 20px)` en el bloque móvil.

## Pendiente
- Dueño: confirmar en el iPhone que el menú muestra la lista completa; borrar el bucket `migracion`.
