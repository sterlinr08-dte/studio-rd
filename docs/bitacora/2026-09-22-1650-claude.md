# 2026-09-22 16:50 — Claude — Reacondicionado: Fase 0 (taller BAYOL CELL) y propuesta simplificada

## Solicitud del dueño
«Ahora el taller de Bayol Cell en la parte de reacondicionado vamos a replicarla y optimizarlo para que una
no técnica pueda entender y simplificar los procesos.»

## Fuente de verdad revisada (solo lectura)
- Repo `sterlinr08-dte/bayolcell-taller` (`taller.html`, 2,7 MB; `CLAUDE.md` §«Módulo REACONDICIONADOS»;
  `CLAUDE_REACOND_UPLOAD.md` con la simplificación a 6 etapas ya aprobada allí).
- Base `vkhwdvjtowrhkhqavnvk`: `refurb_lotes` (7), `equipos_refurbish` (222), `equipo_piezas_pedidas` (250),
  `tareas_trabajo` (725), `equipo_historial` (1.017), `fallas` (44 en 10 categorías), `taller_incentivos` (547),
  funciones `calcular_score_refurb` (100 − penalizaciones) y `detectar_alertas_refurb`.
- Estados internos reales: pendiente → en_evaluacion/evaluado → en_proceso → tecnico_recibio → espera_pieza →
  listo_revision → listo_venta → vendido (+ completado, reasignado, reparacion_externa). 134 vendidos, 59 listos.
- Tareas más frecuentes: cambiar batería (82), subir batería (58), housing (55), pulir pantalla (51), glass (33),
  cambiar marco (33), tapa (26), flex de carga, cámara, pantalla.
- Costo final = compra + parte del flete del lote + piezas. Promedio compra → listo: 11,6 días.

## Propuesta (documento `docs/REACONDICIONADO.md` + maqueta en Claude)
- 6 etapas con una pregunta y un botón principal por pantalla: Recibido · Revisión · En taller · Control de
  calidad · Listo para vender · Vendido (automático al vender el serial).
- Revisión guiada de 10 preguntas sí/no en lenguaje llano → semáforo verde/amarillo/rojo con las mismas
  penalizaciones que Bayol; alertas rojas (cuenta bloqueada, batería inflada, humedad, marco doblado).
- En taller: tareas como botones, piezas del inventario de STUDIO con descuento de stock y costo automático,
  «Falta pieza» bloquea el paso a calidad.
- Listo para vender: precio sugerido por margen configurable; el teléfono pasa a `pos_seriales` disponible con
  su costo final. Ganancia real por teléfono, por lote y por mes.
- Modelo de datos `pos_reacond_*` (5 tablas) + 3 RPC + trigger de venta; sin contabilidad paralela.
- No se replica: Info Plus, costos contables separados, sellado de incentivos, catálogo público.

## Estado
Sin código todavía. Pendiente de aprobación del dueño (maqueta) para construir A) migración, B) frontend
`parches-pos-reacond.js`, C) enlace Compras/Vender, D) QA. Rama `claude/fix-dock-breakpoint-860`.
