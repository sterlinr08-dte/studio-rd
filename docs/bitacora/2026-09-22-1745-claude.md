# 2026-09-22 17:45 — Claude — Publicación 58.94: módulo Reacondicionado en `main`

## Autorización
El dueño, tras ver las capturas del módulo (bitácora 17:30), indicó explícitamente: **«Publícalo»**.

## Coordinación con ChatGPT
Al ir a publicar, `main` ya no estaba en 58.91: ChatGPT publicó **58.92** (notificaciones Toast Premium,
bitácoras 16:00/16:10) y **58.93** (login móvil iPhone, bitácoras 16:30/16:40). La rama
`claude/fix-dock-breakpoint-860` se rebasó sobre `main` (3eadc3c) sin cambios de fondo: solo `APP_VERSION`
y `version.json`. Esta entrega pasa a ser **58.94**. El módulo usa `toast(tp, ttl, msg)` con la misma firma,
así que convive con el toast Premium.

## Contenido publicado (ver bitácora 17:30 para el detalle)
- `parches-pos-reacond.js` (nuevo) + enganches en `parches-pos.js`, `index.html`, `version.json`.
- `studio-brand-theme.css`: `--nx-rail: 0px` (modales del POS en móvil ya no se desplazan 76 px).
- `supabase/studio/17_auditoria_hardening.sql` y `18_reacondicionado.sql` (ambos ya aplicados en la base
  STUDIO antes de publicar; el frontend no los ejecuta).
- Docs: `docs/REACONDICIONADO.md`, bitácoras 16:10, 16:50, 17:30 y esta; `AGENTS.md` estado vivo.

## Pruebas antes de publicar
QA offline en Chromium (escritorio) repetida sobre la rama rebasada: 0 errores JS, 0 recursos 404.

## Publicación
Merge fast-forward de la rama a `main` y push; el deploy lo hace la integración Git de Cloudflare
(Worker `studio-rd`). Verificación de `version.json` en `https://studiord.net` tras el deploy: ver final de
esta entrada.

## Pendientes tras publicar
- Probar con un lote real: «Lote desde compra», «Registrar salida» (IMEI disponible en inventario) y
  descuento de piezas. Son RPC atómicas; si algo falla, el error se muestra en pantalla y no deja estados a medias.
- Técnicos: si un usuario tiene acceso personalizado en Ajustes → Accesos, marcar «Reacondicionado».
- Mantener por rol: Rentabilidad y costos solo se muestran a admin/gerente.

## Verificación en producción (17:55)
- `https://studiord.net/version.json` responde 200 con `"version": "58.94"`.
- `https://studiord.net/parches-pos-reacond.js?v=58.94` responde 200 (293 KB).
- `main` = dd6b170 (+ este commit de bitácora). Deploy por Cloudflare confirmado.
