# 2026-09-23 00:05 — Claude — Publicación 58.96 (incluye 58.95): login nuevo + ajustes de Reacondicionado en `main`

## Autorización
El dueño, tras ver videos y capturas del login (bitácora 2026-09-22 19:00), indicó explícitamente: **«Publícalo»**.

## Coordinación
`main` seguía en `cbd7bb3` (verificación de 58.94); ChatGPT no publicó nada en medio. Merge **fast-forward**
de `claude/fix-dock-breakpoint-860` a `main` sin rebase ni conflictos. `main` = `0b9f204` (+ este commit).

## Contenido publicado
- **58.95** (bitácora 18:10): Reacondicionado — costo final al despachar (`supabase/studio/19_reacond_ajustes.sql`,
  ya aplicado en la base STUDIO antes de publicar), label con código de barras Code128, selector de IMEI del
  inventario al agregar equipo, botón PDF de costos, placeholders.
- Botones compactos de Reacondicionado a escala 34 px (bitácora 18:30).
- **58.96** (bitácora 19:00): login rediseñado (aurora dorada en canvas, partículas, vidrio con borde de luz,
  saludo por hora, Bloq Mayús, sacudida al fallar, destello al entrar, panel de escritorio). `doLogin`,
  `doLoginAuth` y `nxTogglePwd` sin cambios.
- `index.html` `APP_VERSION='58.96'`, `version.json` 58.96.

## Pruebas antes de publicar
`version.json` válido, `node --check parches-pos-reacond.js` OK; QA offline de las bitácoras 18:10/18:30/19:00
(0 errores JS, sin desborde horizontal en 390 px).

## Deploy / verificación
Push a `main` hecho; el deploy lo hace la integración Git de Cloudflare (Worker `studio-rd`).
**No se pudo verificar `https://studiord.net/version.json` desde este entorno**: el proxy de salida bloquea
ahora `studiord.net` (403). Pendiente: confirmar en el navegador que `version.json` responde 58.96 y que el
login nuevo aparece (si se ve el anterior, recargar una vez para tomar la versión nueva).

## Riesgos / pendientes
- Probar el login en iPhone real (Safari) — la QA fue en Chromium.
- Pendientes del dueño sin cambios: lote real de Reacondicionado, borrar bucket `migracion`, activar
  protección de contraseñas filtradas, rotar la clave de Cloudflare.
