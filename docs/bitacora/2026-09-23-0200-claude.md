# 2026-09-23 02:00 — Claude — Publicación 58.98: escala de botones, módulos y menús de acción

## Autorización
El dueño, tras ver las capturas antes/después (bitácora 01:30), indicó: **«Publícalo»**.

## Coordinación con ChatGPT
Al publicar, `main` estaba en `4d45ff4` (58.97, «Login Gold», bitácora `2026-09-22-2052-chatgpt.md`). La rama
`claude/fix-dock-breakpoint-860` se rebasó sobre ese commit: conflicto solo en `version.json` (se conservó la
entrada 58.97 de ChatGPT y se agregó 58.98). `index.html` se fusionó sin conflicto; solo cambia `APP_VERSION`.
El mensaje del commit dice «58.97, sin publicar» porque se escribió antes del rebase; la versión real es **58.98**.

## Pruebas sobre la rama rebasada
- 7 scripts en línea y `parches-pos.js` sin errores de sintaxis.
- Recorrido de los 25 módulos (admin escritorio/móvil, cajero móvil, vendedor escritorio): 0 errores JS, 0 404,
  0 desbordes; permisos iguales.
- Login Gold en Chromium (390 y 1440): sin errores, el error de clave se muestra, la página no se desplaza a los
  lados (el halo del logo queda recortado dentro de `#loginScreen`, que tiene `overflow-x:hidden`).

## Publicación
Fast-forward de `main` a `a653b5e` (+ este commit). Deploy por la integración Git de Cloudflare.
No se pudo verificar `https://studiord.net/version.json` desde este entorno: el proxy de salida bloquea
`studiord.net`. Pendiente: que el dueño confirme que `version.json` dice 58.98.

## Estado
`AGENTS.md` actualizado a 58.98.
