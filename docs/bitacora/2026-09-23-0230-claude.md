# 2026-09-23 02:30 — Claude — «Facturar a» y ventana «Elegir cliente» rediseñadas (58.99, sin publicar)

## Solicitud del dueño
Dos capturas de iPhone (ventana «Elegir cliente» y bloque «Facturar a» de Factura): «Vamos a mejorar ese formato».

## Diagnóstico (captura real)
- Buscador con borde morado de `index.html` (`.nxBusca:focus-within #6d28d9`) + contorno azul del campo: doble anillo.
- Placeholder y botones en MAYÚSCULAS, cortados; campo de 14 px provoca el zoom de Safari al enfocar.
- Filas de ~130 px, todo en mayúsculas, estrella casi invisible, «— CONSUMIDOR FINAL —» como texto suelto,
  emojis 🕒 ⭐ como encabezados.
- «Facturar a»: cuadro de lupa de 64 px + tres líneas de ayuda con emoji; «A crédito» en azul pizarra.
- Nombres con «/ » delante: **dato de la migración** (7 de 399 clientes en `pos_clientes.nombre` empiezan con «/»);
  por eso salen primero en la lista. No se tocó el dato (requiere autorización del dueño).

## Cambios
`parches-pos.js` (solo marcado, sin lógica nueva):
- `nxPosCliSubtxt`: código · teléfono · por mayor. `nxCliIniciales()` para el avatar (ignora signos).
- Filas con avatar de iniciales; «Consumidor final» como fila con icono y «Venta sin cliente asignado»;
  encabezados con iconos (Favoritos, Recientes, «Todos los clientes»); placeholder corto; clase `nxCliPick`.
- `facPartesHTML`: toda la fila del cliente es el botón (`role=button`, teclado Enter/Espacio) con avatar,
  nombre, detalle y «Elegir ›» / «Cambiar ›»; «Ver perfil» detiene la propagación. Ayuda reducida a
  «Sin cliente asignado». `nxFacCliToggle`, `nxPosCliElegir`, favoritos y recientes sin cambios.
`studio-brand-theme.css` §13 / §13b: fila 56 px, avatar 40 px oro suave, control segmentado real (inactivo en
tinta), buscador 44 px con un solo anillo dorado y texto de 16 px (sin zoom en Safari), filas iOS de 56 px,
estrella 44×44 a la derecha (oro si es favorito), hoja al 92 % de alto en el iPhone.
`APP_VERSION` 58.99 + `version.json`.

## Pruebas
Chromium 390 y 1280: abrir, buscar «demo», elegir cliente, ver bloque con cliente. Recorrido completo de los 25
módulos × 4 perfiles: 0 errores JS, 0 404, 0 desbordes. Capturas enviadas al dueño.

## Pendiente
- Decidir si se limpian las «/ » de los 7 nombres migrados.
- Publicar solo con «publícalo».

## Addendum 02:45 — retiro de «IA NEXUS» y publicación
El dueño indicó: **«Quitar nexus IA y publícalo»**.
- `parches-pos.js`: `['ia','IA NEXUS']` sale de `MODULOS` (ya no aparece en Ajustes → Accesos) y `puedeVer('ia')`
  devuelve `false` para todos los roles (también admin). Con eso desaparecen la entrada del menú lateral, la
  sección «Inteligencia» de Inicio (queda vacía y no se pinta) y el acceso directo. El código del módulo
  (`renderIA`, `cargarIAClientes`) queda intacto por si se reactiva (borrar una línea en `puedeVer`).
- Nota en `version.json` 58.99.
- Recorrido: 24 módulos visibles para admin (antes 25), cajero 5, vendedor 8; 0 errores JS, 0 404, 0 desbordes.
- Se publica 58.99 (Facturar a + Elegir cliente + retiro de IA). Las «/» de 7 nombres migrados siguen sin tocar
  (el dueño no autorizó la limpieza de datos).
