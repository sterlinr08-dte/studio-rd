# 2026-09-23 03:05 — Claude — Limpieza de 7 nombres de cliente con «/» (dato migrado)

## Autorización
El dueño: **«Sí, limpia los 7 nombres con "/"»** (ver bitácora 02:30: 7 de 399 clientes migrados tenían el nombre
con «/ » delante y por eso salían primero en la lista).

## Cambio (base STUDIO `edbknlkjnlfmkkiizdbe`)
Revisión previa: ningún nombre limpio choca con otro cliente existente.
```sql
update pos_clientes set nombre = regexp_replace(nombre, '^[\s/]+', '')
where nombre like '/%' and codigo in ('C-00050','C-00051','C-00052','C-00053','C-00054','C-00055','C-00056');
```
7 filas: C-00050 … C-00056. Solo el campo `nombre`; código, cédula, teléfono, saldo y demás sin cambios.
Verificación: 0 clientes con nombre que empiece por «/».

## No tocado
Las 7 facturas históricas (`pos_ventas.cliente_nombre`) guardan el nombre tal como se imprimió en su momento
(«/ …»); es una copia histórica del documento y no se modifica sin autorización específica.

## Frontend
Sin cambios. Los clientes que ya estaban en «Recientes» o «Favoritos» del navegador pueden mostrar el nombre viejo
hasta que se vuelvan a elegir (esa lista es una copia local del dispositivo).
