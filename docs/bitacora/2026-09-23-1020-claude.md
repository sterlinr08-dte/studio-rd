# 2026-09-23 10:20 — Claude — Pantalla de carga con logo de STUDIO (59.01, publicado)

## Solicitud del dueño
Captura del iPhone con la pantalla de carga (escudo azul con ondas): «Vamos a quitar ese logo de nexus».

## Cambio
`index.html`: en `#nxSplash` y `#nxLoader` el escudo `ti-shield` azul con tres ondas (lenguaje de NEXUS PRO) se
reemplaza por `logo-studio.png` (recortado a la franja del logotipo con `object-fit`) y una línea dorada
indeterminada (`.nxs-bar`). Texto «Cargando Studio» / «Preparando tu espacio de trabajo…» en SF, sin mayúsculas.
Respeta `prefers-reduced-motion`. Lógica del splash/loader sin cambios. `APP_VERSION` 59.01 + `version.json`.

## Pruebas
Chromium 390 px: splash y loader con el logo nuevo (capturas enviadas al dueño). Recorrido de módulos admin
escritorio/móvil sin errores.

## Publicación
Autorización vigente del dueño para esta entrega («quitar ese logo»; publicación inmediata como las anteriores del día).
