# 2026-09-23 11:30 — Claude — Factura sin repetidos + bug de la X en los avisos (59.05, sin publicar)

## Pedido del dueño
Captura móvil de Factura: «¿Qué me sugieres quitar? El botón de cobrar se repite dos veces». Tras la propuesta:
**«Hazlo»**, más una captura de un aviso «Agregado» con un icono rayado encima de la X.

## Cambios
1. `studio-brand-theme.css` §16: con la barra inferior fija (`.nxFacBar`), en `#facResumen` se ocultan `.acc`
   (Cobrar / Guardar / imprimir) y `.cancel`; el Total queda del tamaño del desglose. Cobro y total grande solo en la
   barra. Atajos F2/F10 (`.keys`) ocultos en táctil / ≤ 860 px. Se dejan «Limpiar carrito» (solo artículos; la ✕
   de la barra cancela todo) y el 💾 (borrador).
2. `index.html` (toast premium de 58.92): las piezas `.ti-ic/.ti-b/.ti-t/.ti-m/.ti-x` chocaban con clases de
   Tabler Icons; `.ti-x::before` pintaba el glifo de la X sin la fuente → bloque rayado. Se anula el `::before` de
   las piezas (no el del `<i class="ti …">` interior).
3. Con la barra de cobro, los avisos suben por encima de ella (76 px; 84 px en móvil).
`APP_VERSION` 59.05. Incluye lo de 59.04 (tabla en dos líneas; «Agregar artículo» en modo lista).

## Pruebas
Chromium 390 y 1280: «Cobrar» visible solo en la barra; tarjeta con desglose; F2/F10 ocultos en móvil y visibles en
PC; `::before` de la X del aviso = none. Recorrido de módulos × 4 perfiles: 0 errores JS, 0 404.

## Publicación
Pendiente de «publícalo».
