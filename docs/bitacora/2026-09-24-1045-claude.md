# 2026-09-24 10:45 — Claude — Barra de acciones alineada con la ventana y botones compactos · 59.15 sin publicar

## Solicitud del dueño
Foto de la pantalla de Factura en su computadora con el pedido: «Vamos a poner que la barra de botones quede al mismo nivel de la ventana de factura y ajustar el tamaño de los iconos». En la foto, la barra fija era más ancha que la tarjeta de la factura: salía por la izquierda y por la derecha.

## Diagnóstico
En escritorio la barra `.nxFacBar` tenía posición fija con `left: 288px; right: 24px` (§ ajustes 22-sep). No seguía la tarjeta `.nxDocCard`, que está centrada con ancho máximo, así que en pantallas anchas la barra quedaba más ancha. Los botones medían 44 px de alto, con iconos de 17 px y fuente de 13.5 px.

## Cambios
**`parches-pos.js`: `nxBarAlinear()`**
- Al montar la barra (`nxStickyBarSet`), copia el `left` y el `width` reales de la tarjeta del documento: `#v-pos .nxDocCard` en Factura y Prefactura, `#v-pos .nxPrForm` en Compra.
- Se recalcula con `ResizeObserver` (tarjeta y body) y al cambiar el tamaño de la ventana.
- En menos de 861 px sigue de borde a borde, como antes.

**`studio-brand-theme.css` §19**
- Botones de 38 px, iconos de 15 px, fuente de 13 px, radio de 9 px, espacio de 6 px; total de 18 px.
- Qué botones se muestran ahora depende del **ancho de la barra** (`@container`), no del ancho de la pantalla:
  - hasta 1120 px ocultan las teclas;
  - hasta 940 px Cancelar y Anular pasan al menú ⋯;
  - hasta 720 px solo se ven los iconos;
  - hasta 520 px se oculta Imprimir.

## Pruebas (Playwright)
Alineación, barra comparada con la tarjeta:

| Pantalla | Barra | Tarjeta |
|---|---|---|
| 1440 | 325→1354 | 325→1355 (6 botones con texto) |
| 1280 | 280→1240 | 280→1240 (⋯ + 4 botones) |
| 1000 | 280→960 | 280→960 (iconos) |
| Móvil 390 | 0→390 | — (de borde a borde) |

**Regresión**
- Guardar+WhatsApp, ticket, carta, anular.
- Prefactura y Compra.
- 0 errores.

## Estado
- Rama `claude/fix-dock-breakpoint-860`.
- 59.15 **sin publicar**; `main` en 59.14.
