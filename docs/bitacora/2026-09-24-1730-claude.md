# 2026-09-24 17:30 — Claude — Financiamiento fase 5: datos legales del contrato (NEXUS PRO) · migración 29 aplicada; 59.19 sin publicar

## Solicitud del dueño
«Sí» a seguir, en orden, con: 1) configuración legal del contrato, 2) reportes NEXUS, 3) cuota fija francesa.

## Fuente de verdad (auditoría)
**NEXUS PRO**
- `nxPrestamoConfig` / `prestamos_config` (~3265) guarda:
  - acreedor (nombre, RNC, teléfono, dirección);
  - abogado (nombre, cédula, matrícula CARD, teléfono);
  - 2 testigos.
- El contrato (~3370–3425) imprime:
  - EL ACREEDOR y EL DEUDOR;
  - «Hecho y firmado de buena fe… ante los testigos»;
  - firmas de los testigos;
  - «LEGALIZACIÓN DE FIRMAS… CERTIFICO Y DOY FE…».
- NEXUS lee la configuración **al imprimir**.

**STUDIO**
- El texto del contrato ya se congelaba al crear el financiamiento (`pos_fin_crear_financiamiento_v2` → `contrato_texto`).
- No había datos de acreedor, notario ni testigos.

## Migración `supabase/studio/29_fin_contrato_legal.sql`
Aplicada como `studio_29_fin_contrato_legal` + `29b`.
- 12 columnas `fin_*` nuevas en `pos_config`: acreedor, abogado y testigos.
- `pos_financiamientos.contrato_legal` (jsonb): los datos legales **congelados por contrato**. Mejora sobre NEXUS: un cambio de abogado no altera contratos ya emitidos.
- `pos_fin_legal_snapshot(org)` arma el jsonb.
- Trigger `pos_fin_zz_legal_al_crear` (BEFORE INSERT) congela los datos al crear. El nombre «zz» es para que corra después de `pos_fin_org`, que asigna la organización.
- `pos_fin_contrato_legal_fijar(fin)`, para usuarios autenticados de la organización: congela los datos la primera vez que se imprime un contrato creado antes de configurarlos. Solo actúa si todavía está vacío.
- **Prueba con rollback:**
  - al insertar, el financiamiento congeló «Lic. Prueba Notario»;
  - se cambió el abogado en la configuración y se vació el campo;
  - `fijar` tomó el abogado nuevo.
  - Todo se revirtió.

## POS (`parches-pos.js`)
**Planes (solo admin o gerente)**
- Tarjeta «Datos legales del contrato» con 12 campos.
- Guarda en `pos_config`.
- Avisa si hay notario sin matrícula CARD.

**`nxFinV2Contrato`**
- Toma `contrato_legal`; si está vacío, lo fija con la RPC.
- Título con el nombre del acreedor en mayúsculas.
- Recuadros EL VENDEDOR (ACREEDOR) y EL COMPRADOR (DEUDOR), con cédula, domicilio y teléfono.
- Cierre «Hecho y firmado de buena fe…» con la fecha en letras.
- Firma «Por <acreedor> · EL VENDEDOR».
- Firmas de testigos.
- Legalización de firmas del notario, con el texto de NEXUS.
- Se mantienen la cláusula del fiador (fase 2), la firma electrónica y el anexo del expediente.

## Pruebas (Playwright)
- Tarjeta con 12 campos; guardar hace PATCH de `pos_config`.
- Contrato con partes, fiador, cierre, firmas, testigos, legalización (CARD 45678) y fecha en letras.
- Financiamiento por 4 roles, 0 errores.

## Estado
- 59.19 **sin publicar**; `main` en 59.18.
- Sigue la fase 6: reportes.
