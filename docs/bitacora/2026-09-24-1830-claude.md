# 2026-09-24 18:30 — Claude — Financiamiento fase 7: cuota fija (sistema francés) en «sobre saldo» · migración 30 aplicada; 59.21 sin publicar

## Solicitud del dueño
Última de las tres fases aprobadas con «Sí»: alinear el método «saldo» a la cuota fija de NEXUS.

## Auditoría previa (fuente de verdad)
**`pos_fin_amortizacion`** (servidor)
- Genera las cuotas al crear el financiamiento (`pos_fin_crear_financiamiento_v2`).
- Arma la tabla de la solicitud (`pos_fin_amortizacion_plan`, `pos_fin_sol_ver`).
- En «saldo» repartía el capital en partes iguales y cobraba el interés sobre el saldo, así que la cuota bajaba cada período.

**Tasa del plan**
La tasa de STUDIO es **por cuota** (por período). Hay dos fases: `tasa1` para las primeras `cuotas_fase1` y `tasa2` después.

**NEXUS** (`nx-fin.js amortizar`)
Cuota fija = C·i/(1−(1+i)^−n).

**Datos**
- **0 planes, 0 solicitudes, 0 financiamientos y 0 cuotas.**
- El cambio no toca ningún dato ni ningún asiento contable.

## Cambios
**Migración `supabase/studio/30_fin_cuota_fija.sql`** (aplicada como `studio_30_fin_cuota_fija`)
- Misma firma y el mismo `TABLE(...)` que antes.
- «saldo»: cuota fija con la tasa por cuota, recalculada al empezar la fase 2 sobre el saldo que queda.
- La última cuota ajusta los centavos.
- «plano» no cambia.

**`parches-pos.js`**
- `finV2Amortizar` es un espejo exacto del servidor; es lo que usan la vista previa, la solicitud, la evaluación y el plan de ejemplo.
- Textos: «Cuota fija (sobre saldo)» en el selector del plan y «cuota fija · interés sobre saldo» en la ficha.

## Pruebas: servidor contra POS, idénticos centavo a centavo
| Caso | Resultado |
|---|---|
| A: 100,000 · 12 cuotas · 5 % | 11 cuotas de **11,282.54** y la última de 11,282.55. Es el valor estándar del sistema francés; el interés baja de 5,000 a 537.26. |
| B: 40,000 · 4 cuotas · fase 1 = 2 al 10 %, fase 2 al 5 % | 12,618.83 ×2, luego 11,778.17 y 11,778.18 |
| C: 9,000 · 3 cuotas · 0 % | 3,000 ×3 |
| D: plano 40,000 · 4 · 5 % | 12,000 ×4 (sin cambios) |

Regresión: financiamiento por 4 roles, evaluación (fase 2), tablero (fase 6) y Factura. 0 errores.

## Estado
- 59.21 **sin publicar**; `main` en 59.18.
- Listas para publicar juntas: 59.19 (datos legales), 59.20 (reportes) y 59.21 (cuota fija).
- Con esto queda completa la réplica del módulo de financiamiento de NEXUS PRO acordada con el alcance «Artículos, con todo NEXUS».
