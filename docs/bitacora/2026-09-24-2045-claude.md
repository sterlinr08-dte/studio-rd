# 2026-09-24 20:45 — Claude — IMEI vendidos al aprobar financiamiento + publicación 59.24

## Autorización
El dueño respondió «Si» a: (1) publicar la solicitud con buscadores de Factura como 59.24 y (2) corregir el IMEI al aprobar.

## Diagnóstico (ver hallazgo en `2026-09-24-2030-claude.md`)
- `pos_fin_aprobar_solicitud` llamaba a `pos_registrar_venta_atomica(..., p_reserva_token => null, p_imei_esperados => 0)`:
  el IMEI solo quedaba como texto; `pos_seriales` seguía «disponible» y `pos_aplicar_inventario_venta` (que en
  artículos con IMEI descuenta por seriales vendidos) no rebajaba ese teléfono. 292 artículos tienen IMEI; 0
  solicitudes pendientes y 0 financiamientos a la fecha, así que no hay datos históricos que reparar.

## Cambio — migración 32 (`supabase/studio/32_fin_aprobar_imei.sql`, aplicada como `studio_32_fin_aprobar_imei`)
- Misma firma y mismo flujo. Nuevo, antes de crear la venta:
  - `FIN_IMEI_FALTANTE` si en un artículo con IMEI la cantidad ≠ número de IMEI;
  - reserva los IMEI (disponibles, sin venta) con un token; `FIN_IMEI_NO_DISPONIBLE` si falta alguno;
  - sin almacén elegido, usa el almacén donde están los IMEI si es uno solo;
  - pasa token y cantidad a `pos_registrar_venta_atomica`, que los confirma como vendidos con la venta (mismo camino de Factura).
- Todo en una transacción: si algo falla no queda nada reservado ni vendido.
- Frontend: mensajes en español para `FIN_IMEI_FALTANTE`, `FIN_IMEI_NO_DISPONIBLE`, `VENTA_IMEI_OTRO_ALMACEN`.

## Pruebas (base real, transacción revertida)
- Sin IMEI → FIN_IMEI_FALTANTE. IMEI inexistente → FIN_IMEI_NO_DISPONIBLE.
- IMEI válido → aprobada; IMEI «vendido» con `venta_id` de la venta; venta en el almacén del IMEI; stock 78→77 y
  stock del almacén 71→70. Verificado después: IMEI sigue «disponible», stock 78, 0 planes, 0 solicitudes (nada persistió).

## Publicación
- `APP_VERSION` / `version.json` → 59.24; `AGENTS.md` actualizado; fast-forward a `main` (Worker `studio-rd`).
