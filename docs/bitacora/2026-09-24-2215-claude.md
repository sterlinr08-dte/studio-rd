# 2026-09-24 22:15 — Claude — CRM Fase 1 (réplica mejorada del CRM de Bayol Cell, sin canales externos)

## Solicitud del dueño
«Vamos a replicar el CRM de bayol cell taller y aplicarle mejoras» → «Si» a la Fase 1 propuesta
(auditoría en `2026-09-24-2115-claude.md`). La pregunta del número de WhatsApp para la Fase 2 sigue abierta.

## Fuente de verdad
Se AMPLÍA `pos_crm` (tablero existente de STUDIO, 0 registros); no se crea un embudo paralelo.

## Base de datos — migración 33 (`supabase/studio/33_crm_fase1.sql`; aplicada como `studio_33_crm_fase1` + `studio_33b_crm_historial_fix`)
- `pos_crm`: `asignado_id/asignado_nombre`, `interes`, `motivo_perdida`, `cotizacion_id`, `venta_id`, `reparacion_id`,
  `financiamiento_id`, `etapa_at`, `actualizado_at` + índices.
- `pos_crm_actividades`: nota, llamada, whatsapp (manual), visita, tarea (con fecha, responsable, hecha), etapa y sistema.
- Triggers (servidor): etapa válida; «perdido» exige motivo; `cerrado_at` al ganar/perder (se limpia al reabrir);
  `etapa_at`; nombre del asignado; historial automático de cambios de etapa y de asignación; historial NO editable ni borrable.
- RLS (mejora sobre Bayol, que filtraba en el navegador): admin/gerente ven todo; los demás ven lo asignado a ellos y
  lo libre, solo pueden tomar lo libre para sí o soltar lo suyo (`CRM_ASIGNAR_SOLO_ADMIN`); solo admin/gerente borran.
  Las actividades heredan la visibilidad de su oportunidad.
- RPC `pos_crm_usuarios()` (id, nombre, rol de usuarios activos de la organización; sin datos de acceso).
- 33b: corrige una variable que chocaba con la columna `nom` en el trigger de historial (detectado por la prueba).

## Frontend
- Nuevo `parches-pos-crm.js` (cargado después de Reportes en `index.html`): pestañas Tablero / Tareas / Reportes.
  - Tablero de 5 etapas con arrastrar y soltar, botón «avanzar» (móvil), días en la etapa, próxima tarea, responsable,
    filtros Todas / Mías / Sin asignar y buscador. Ganado/Perdido muestran 30 días.
  - Ficha: etapas, datos, cliente con el selector de Factura, responsable (admin) o «Tomarla/Soltarla», WhatsApp que
    solo abre wa.me y registra la actividad (nunca envía nada), permiso de mensajes (`pos_clientes.acepta_whatsapp`),
    documentos: «Cotizar» y «Facturar» enlazan la cotización/factura que se guarde (y pasan a Cotizado/Ganado),
    reparación vinculable; actividad con notas, llamadas, visitas y tareas con fecha y responsable.
  - Perder pide motivo (lista + texto libre).
  - Reportes por rango: creadas, ganadas, perdidas, conversión, ciclo promedio, embudo actual, motivos de pérdida,
    por vendedor y por fuente.
- `parches-pos.js`: el CRM viejo queda como respaldo si el módulo no carga; `nxPosCtx` expone clientes, selector de
  cliente, `nextSeq`, abrir Factura/Cotización con cliente; ganchos `docGuardado` y venta creada; Avisos muestra
  «Tareas del CRM — vencidas y de hoy».

## Pruebas
- Base real (transacción revertida, usuarios admin y vendedor reales): vendedor ve 2 de 3; no modifica ajenas (0 filas);
  toma una libre; no asigna a otro; «perdido» sin motivo rechazado; `cerrado_at` puesto; no puede insertar ni editar
  historial; no puede borrar; no ve actividades de oportunidades ajenas; historial generado solo. Nada persistió (0/0).
- Playwright 1280 y 390, admin y vendedor: tablero, ficha, motivo, nueva, tareas (vencidas/hoy), reportes, Avisos; sin errores.

## Estado
Rama `claude/fix-dock-breakpoint-860`, sin publicar (sería 59.25).
