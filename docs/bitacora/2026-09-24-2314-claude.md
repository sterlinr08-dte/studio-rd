# 2026-09-24 23:14 RD — Claude — Vitrina horizontal de colores (movilidad), prueba sin publicar

> Nota de fecha: esta hora es la real de America/Santo_Domingo según el reloj del contenedor. Hay entradas anteriores con nombre `2026-09-25-…` que quedaron adelantadas; no se tocaron.

## Solicitud del dueño
Mejorar la visualización del catálogo siguiendo un video de ejemplo: una tarjeta grande por producto, pintada del color del producto, con el producto flotando en el centro, las características en píldoras y las opciones en círculos. Pidió empezar por la LQ-N9 y que las tarjetas **se desplacen de manera horizontal**.

## Qué se hizo (solo `tienda.html`)
- **Sección Movilidad → vitrina horizontal.** Las 8 pasolas pasan de presentaciones verticales a un carrusel horizontal con `scroll-snap`: una tarjeta por modelo que se desliza con el dedo, con flechas en computadora y puntos con contador (`01 / 08`). Monta cargas, Hogar y Tecnología siguen igual.
- **Cada tarjeta tiene su color.**
  - Se agregó `tono` a cada artículo y `tonos` por variante en la LQ-M12.
  - Tonos: N9 `#1784c0`, H8 `#8c1c2a`, ZS `#2745b3`, M12 rosa/blanco/gris, XGW `#9d1b25`, HLD `#d23a2f`, PP `#9fd2bf`, XBW `#1f93d4`.
  - En colores claros (rosa, blanco, menta) el texto pasa a oscuro automáticamente según la luminancia.
  - La franja de fondo se tiñe con el tono de la tarjeta que está al frente.
- **Composición de la tarjeta.**
  - Nombre grande con subtítulo y características en píldoras.
  - Producto al centro, con luz y sombra del mismo tono y el código del modelo gigante detrás.
  - A la derecha, los datos (motor, velocidad, autonomía) y, si el modelo tiene variantes, los colores en círculos.
  - Abajo, el precio o «Resérvalo» y los botones de WhatsApp.
- **Móvil:** una columna. Los datos van en fila y las características en una fila deslizable.
- **Se reutiliza sin cambios de lógica:**
  - el cambio de variante: foto, estado, precio y mensaje, y ahora también el tono;
  - el visor 360° de N9 y H8;
  - los enlaces de «Explora», que centran la tarjeta;
  - el índice lateral, que cuenta solo la tarjeta al frente.
- Respeta `prefers-reduced-motion`. No hay dependencias nuevas.

## Pruebas (Chromium, 1280×820 y 390×844)
- Sin errores de página y sin scroll horizontal del documento (`scrollWidth` = ancho de la ventana).
- Flechas y puntos cambian de tarjeta, y la franja cambia de tono.
- Variante Rosa de la M12: tono claro, texto oscuro y botón «Pedir precio» legible.
- Botón 360° de la N9: carga y gira; al salir de la tarjeta vuelve a la foto.
- El enlace de «Explora» a `#p-lq-pp` centra esa tarjeta.

## Pendiente / riesgos
- En modo 360° se ve el plato oscuro de las fotos de giro sobre el color; se podría limpiar después.
- **No publicado.** Rama `claude/fix-dock-breakpoint-860`. No se cambió la versión (`APP_VERSION`/`version.json`); se hará al publicar.

## Siguiente paso
Que el dueño vea las capturas. Si lo aprueba: publicar y decidir si se aplica el mismo formato a Monta cargas, Hogar y Tecnología.
