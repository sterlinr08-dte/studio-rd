# 2026-09-24 23:30 — Claude — CRM Fase 2: bandeja única de WhatsApp, Instagram y Facebook (lista, apagada)

## Solicitud del dueño
«Si su propio número de whatsapp su Facebook e Instagram» → STUDIO usará cuentas PROPIAS (no las de Bayol Cell).

## Diagnóstico / fuente
- Bayol Cell: Edge Functions `whatsapp-webhook`, `whatsapp-enviar`, `instagram-*`, `social-*` sobre Zernio
  (`https://zernio.com/api/v1/inbox/conversations/{id}/messages`, firma `X-Zernio-Signature`), tablas separadas por canal.
- STUDIO no tenía ninguna función ni tabla de mensajería.

## Base de datos — migración 34 (`supabase/studio/34_crm_bandeja.sql`; aplicada como 34, 34b, 34c, 34d)
- `crm_canales` (cuenta Zernio por plataforma, nace `activo=false`), `crm_conversaciones` (única por canal + contacto),
  `crm_mensajes` (único por id del proveedor; clave de idempotencia única), `crm_webhook_eventos` (evento crudo, solo servidor).
- Triggers: resumen de conversación solo al insertar (no leídos no se duplican); estados de entrega solo avanzan;
  asignación con las reglas del CRM; desde la app no se pueden alterar canal/contacto/ids/fechas ni subir no leídos.
- RLS: conversaciones visibles según `pos_crm_puede` (admin/gerente todo; resto lo suyo y lo libre); mensajes heredan;
  canales: todos leen, admin/gerente activan; eventos sin acceso desde la API.
- Bucket privado `crm-media` (lectura solo usuarios del sistema; subida solo a `salientes/`).
- `crm_cliente_por_telefono` (servidor): vincula sola una conversación de WhatsApp al cliente por los últimos 10 dígitos.
- 34c/34d: search_path fijo, funciones de trigger sin EXECUTE para la API, restricción solo para cambios directos de la app.

## Edge Functions (desplegadas en STUDIO)
- `crm-webhook` (sin JWT; firma HMAC obligatoria): guarda el evento primero, deduplica, procesa mensajes entrantes,
  espejos del teléfono y estados; cuenta desconocida → se registra APAGADA; canal apagado → no procesa; error → 500
  para que Zernio reintente. Primer mensaje de un contacto nuevo → crea oportunidad «Nuevo» en el CRM, enlazada.
  Correcciones de Bayol F15/F16/F17/F19 incorporadas. Nunca envía nada.
- `crm-enviar` (JWT): visibilidad con la RLS del usuario, canal activo, ventana de 24 h en WhatsApp, registro previo con
  clave de idempotencia, reintento solo en `CONVERSATION_NOT_FOUND`, `fallido` con el error si Zernio falla.
- Verificado en vivo (vía pg_net): `crm-webhook` responde 503 `webhook_secret_not_configured` → inerte hasta configurar.

## Frontend (`parches-pos-crm.js`)
- Pestaña **Bandeja** (primera del CRM): lista con filtros (No leídas, Sin responder, Mías, Sin asignar, por canal),
  chat con burbujas, estados (enviando/enviado/entregado/leído/no se envió), adjuntos con URL firmada, enviar texto o
  archivo (16 MB), vincular cliente, abrir o crear oportunidad, tomar/soltar/asignar, archivar; aviso de ventana de 24 h
  y de canal apagado; panel «Canales» (admin) para activar/apagar cada cuenta. Actualiza cada 10 s sin borrar lo escrito.

## Pruebas
- Base real (transacciones revertidas): duplicado rechazado; no leídos 2 → 0 al responder; estado leído no retrocede
  ni se pisa con fallido; vendedor ve 1 de 2, no modifica ajenas, no asigna a otro, no ve mensajes ajenos, no inserta
  mensajes; campos protegidos intactos; triggers siguen funcionando tras revocar EXECUTE. Nada persistió (0 en todas).
- Playwright 1280/390, admin y vendedor: lista, chat, envío (llama a crm-enviar con clave), ventana cerrada, canal apagado.

## Pendiente del dueño para encenderla (nada de esto lo hace Claude)
1. En Zernio, conectar el WhatsApp Business, la página de Facebook y el Instagram de STUDIO (idealmente en un espacio
   de Zernio propio de STUDIO, para no mezclar el webhook con Bayol Cell).
2. En Zernio, apuntar el webhook a `https://edbknlkjnlfmkkiizdbe.supabase.co/functions/v1/crm-webhook`.
3. En Supabase STUDIO → Edge Functions → Secrets: `ZERNIO_API_KEY` y `ZERNIO_WEBHOOK_SECRET` (nunca por chat).
   Recordatorio: en Bayol sigue pendiente rotar su llave de Zernio expuesta; no reutilizarla.
4. Cuando llegue el primer mensaje, activar cada canal en CRM → Bandeja → Canales y probar SOLO con el número del dueño.

## Estado
Rama `claude/fix-dock-breakpoint-860`, sin publicar el frontend (sería 59.26). Las funciones y la base ya están en el servidor, inertes.
