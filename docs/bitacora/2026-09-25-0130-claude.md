# 2026-09-25 01:30 — Factura: asistente compacto (Claude)

Sigue a `2026-09-25-0115-claude.md`. El dueño dijo que el cuadro se estaba poniendo muy grande.

## Cambios (solo presentación; la lógica del asistente no cambia)
- **Avisos**: cada uno ocupa **una línea** (unos 36 px). El texto completo va en el título.
  - Se ven **2 avisos**; el resto queda en «+N avisos más» / «Ver menos».
  - «Suelen llevar» muestra 2 sugeridos en línea.
- **Acciones**: **una fila de píldoras** de 32 px que se desliza de lado, con un difuminado que
  indica que hay más. Antes eran 2 filas de tarjetas de 58 px.
- **Últimas facturas**: 3 facturas, una línea cada una. Imprimir y Anular son iconos discretos.
- Se quitan los rótulos «Para esta venta» y «Acciones».

## Resultado medido (iPad 820 / 1280)
El panel baja a unos 305 px de alto, cerca de la tarjeta del total (unos 220 px). Antes medía unos
560 px con avisos.

## Archivos
- `parches-pos.js`: `facOtrasHTML`, `facSugHTML`, `nxFacSugTodas`, límite de 3 en las últimas
  facturas.
- `studio-brand-theme.css`: bloque `.facQ*` / `.facSug*` / `.facUlt*` reescrito.

## Pruebas
Playwright con Supabase simulado. Vistas: 390, 820 y 1280. 0 errores; los flujos anteriores
(retomar, agregar un sugerido, anular) siguen funcionando.

## Estado
En la rama. **No publicado.**
