# 2026-09-25 01:45 — Factura: panel aún más pequeño y «Facturar a» sin aplastarse (Claude)

Sigue a `2026-09-25-0130-claude.md`. El dueño respondió «Hazlo» a la propuesta de quitar
«Últimas facturas».

## Cambios
- **Se quita «Últimas facturas»** del panel. Se llega a las facturas con la acción «Buscar», la
  lupa del número y el Anular de la barra. Las funciones `facUltHTML` y `facUltCargar` quedan sin
  uso visible; no hacen consultas si el bloque no está en pantalla.
- El panel se alinea arriba (`align-self: start`) y ya no se estira al alto del total. Mide unos
  168 px contra los 220 px del total en iPad y 1280.
- **Arreglo existente también en producción**: con un cliente elegido, en iPad (820 px) la columna
  «Facturar a» quedaba muy angosta, con el nombre partido letra por letra. La causa: la columna
  «Condición de pago» medía `auto` y la línea «Última compra…» la estiraba. Ahora mide
  `fit-content(45%)`.

## Archivos
- `parches-pos.js`: se quita el bloque `.facUlt` de `renderFactura`.
- `studio-brand-theme.css`: `.facOtras` con `align-self: start`, y `.partes` con
  `fit-content(45%)`.

## Pruebas
Playwright en 390, 820 y 1280 con Supabase simulado.
- 0 errores.
- Anular desde la barra sigue abriendo «¿Qué factura vas a anular?».
- Comparación con `origin/main`: el aplastamiento de «Facturar a» existía en producción y queda
  corregido.

## Estado
En la rama `claude/fix-dock-breakpoint-860`. **No publicado**: se espera un «sí» explícito.
