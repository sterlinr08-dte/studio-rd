# 2026-09-25 02:30 — Página pública de STUDIO en la portada de studiord.net (Claude)

## Solicitud del dueño
Continuar la página web (`tienda.html`, diseñada el 25-sep). Datos que dio el dueño:
- dirección: Av. Jacinto B. Dummit, frente a Auto Repuesto Santiago, Santiago;
- teléfono: 809-570-6254;
- WhatsApp: 829-624-7623;
- horario: lunes a sábado de 8:30 a 6:30; domingo cerrado;
- ubicación: **portada de studiord.net**.

## Cambios
- `tienda.html`
  - Datos reales: WhatsApp (todos los botones abren `wa.me/18296247623` con su mensaje),
    teléfono (`tel:`), dirección con enlace a Google Maps y horario.
  - «Santiago» en la portada, el título y el pie.
  - Datos estructurados `ElectronicsStore` para buscadores.
  - «Acceso del personal» apunta a `/app`.
  - Las secciones que aparecen al bajar ya no quedan ocultas si el código de la página no carga.
- `worker.js` (antes solo pasaba todo a los archivos):
  - `/` muestra la **tienda**, salvo en equipos con la marca `studio_staff=1` o con `?v=` (la
    recarga de actualización del sistema). En esos casos muestra la app.
  - `/app` y `/index.html` muestran **siempre la app**, sin redirigir. La app instalada arranca en
    `index.html` y en iPhone no comparte cookies con Safari.
  - `/app/` redirige a `/app`.
  - Las respuestas de `/` llevan `Cache-Control: no-cache` y `Vary: Cookie`.
  - El resto (JS, imágenes, `verificar.html`, firma) no cambia.
- `index.html`: `nxMarcarPersonal()` pone la cookie `studio_staff=1` (sin datos, 1 año) al
  iniciar sesión (3 puntos donde se guarda `nx_sesion`) y al cargar con sesión abierta.

## Efecto para el personal
- En equipos donde ya entraron al sistema: sin cambio.
- Primera vez después del cambio sin sesión abierta: ven la tienda y usan «Acceso del personal»
  (o `studiord.net/app`). Después de entrar, ese equipo vuelve a abrir el sistema en
  `studiord.net`.
- La app instalada en el teléfono no cambia.

## Pruebas
- Worker probado con un ASSETS simulado que redirige como Cloudflare (`/index.html` → `/`):
  11 casos correctos (tienda, app con cookie, `?v=`, `/app`, `/index.html`, `/app/` 301,
  archivos).
- Playwright de `tienda.html` en 1280 y 390: sin desplazamiento lateral, enlaces de WhatsApp y
  teléfono correctos, 0 errores.
- El sistema arranca normal (Factura sin errores).

## Cómo revertir
En `worker.js`, dejar solo `return env.ASSETS.fetch(request);`.

## Estado
En la rama `claude/fix-dock-breakpoint-860`. **No publicado**: se espera un «sí».
