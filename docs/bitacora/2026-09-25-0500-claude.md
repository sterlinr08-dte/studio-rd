# 2026-09-25 05:00 — Publicación 59.28: página pública en la portada de studiord.net (Claude)

El dueño autorizó con «Si» publicar la página mostrada en `2026-09-25-0450-claude.md`.

## Qué se publica
- `tienda.html`: página comercial futurista.
  - Una presentación con movimiento para cada uno de los 16 artículos de su catálogo de cliente
    final.
  - Sin precios (`MOSTRAR_PRECIOS = false`).
  - WhatsApp 829-624-7623, teléfono 809-570-6254, dirección Av. Jacinto B. Dummit y horario.
  - Ver las bitácoras `0300` a `0450`.
- `img/tienda/` (fotos sin fondo y `og-studio.jpg`).
- `worker.js`:
  - `/` muestra la tienda, salvo a equipos con la cookie `studio_staff=1` o con `?v=`;
  - `/app` e `/index.html` siempre muestran el sistema, sin redirigir.
  - Ver `2026-09-25-0230-claude.md`.
- `index.html`: marca `studio_staff=1` al iniciar sesión (`nxMarcarPersonal`).
- `APP_VERSION`, `version.json` y `AGENTS.md` pasan a 59.28.

## Efecto para el personal
- Donde ya iniciaron sesión después del cambio, `studiord.net` sigue abriendo el sistema.
- La primera vez sin la marca: «Acceso del personal» (al pie) o `studiord.net/app`.
- La app instalada en el teléfono arranca en `index.html` y no cambia.

## Cómo revertir
En `worker.js`, dejar solo `return env.ASSETS.fetch(request);`. La portada vuelve a ser el
sistema.

## Pendiente
- Revisar en vivo después del deploy de Cloudflare: la portada, `/app` y la app instalada.
- El dueño decide cuándo mostrar los precios y si se agrega la garantía.
