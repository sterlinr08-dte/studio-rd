# Claude — Tienda: limpieza del código sin uso (sin publicar)

## Solicitud del dueño
«Límpialo»: quitar el código viejo que quedó sin uso tras la 59.46.

## Qué se quitó (`tienda.html`, 88 KB → 76 KB)
- **JavaScript:**
  - el script de movimiento de las antiguas presentaciones (armado pieza por pieza, entradas de `.pres`, nombre gigante con el scroll, índice activo e inclinación de `.escenario` con el mouse); queda solo la entrada de los bloques `.aparece` y la barra de lectura de la cabecera;
  - en el cambio de variante, las ramas para `.pres` (`con-giro`, `.brillo`);
  - `esVista360`;
  - los campos `gigante` y `titulo` de `SECCIONES`, que ya no se muestran.
- **CSS:**
  - 127 reglas y 7 `@keyframes` que no corresponden a ningún elemento de la página;
  - detectadas con tinycss2 comprobando cada selector contra el HTML y el JS; la lectura y reescritura se verificó idéntica antes de filtrar;
  - incluye `.pres`, `.divisor`, `.escenario`, `.ficha`, `.dato`, `.brillo`, `.ensamble`, `.hud`, `.anillo` y restos de la portada anterior.
- **Regla rota:** la limpieza de la 59.46 (borrado por líneas) dejó suelto el final de la regla `.tira`, y ese fragmento anulaba la regla siguiente (`.divisor`). Como `.divisor` ya no existía en la página, en vivo no tuvo efecto visible. Queda eliminado.

## Pruebas (Chromium 390×844 móvil y 1280×820)
- **Comparación píxel a píxel contra `origin/main` (59.46):** página completa, las 4 pestañas y la LQ-M12 en gris dan **idéntico**, con movimiento reducido y el video oculto para que sea determinista.
- 360° de la N9, pestañas, enlaces de cabecera, entrada `#p-nevera-cw` y `scrollX` = 0: correctos y sin errores de página.

## Estado
Sin publicar. Rama `claude/fix-dock-breakpoint-860`.
