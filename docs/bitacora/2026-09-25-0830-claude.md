# 2026-09-25 08:30 — Ícono de STUDIO en lugar del escudo (Claude)

El dueño, con una captura de las pestañas de Safari: cambiar el logo del escudo.

## Diagnóstico
`icon-16/32/192/512` e `icon-apple-*` seguían siendo el escudo heredado de NEXUS PRO. Los usan la
pestaña del navegador, la app instalada (`manifest.json`, `apple-touch-icon`), `tienda.html`,
`verificar.html` y `firma-financiamiento.html`.

## Cambios
- Íconos regenerados: la «S» del logo `logo-studio.png`, blanca sobre negro #0A0A0A, con una raya
  dorada debajo y un brillo dorado suave. 16 y 32 px con esquinas redondeadas; iOS redondea el
  resto. Los originales quedaron guardados en el scratchpad de la sesión (no en el repositorio).
- Enlaces a los íconos con `?v=2` en los 4 HTML y en `manifest.json`, para que Safari no muestre
  el escudo guardado.
- `sw.js`: `CACHE_NAME` pasa a `studio-rd-v2` y borra la caché de imágenes vieja.
- `APP_VERSION` y `version.json` pasan a 59.32.

## Nota
Las capturas del dueño todavía mostraban la versión anterior (letra ancha, sin «Acceso personal»):
es la caché de Safari. Una app ya instalada en la pantalla de inicio puede conservar el ícono viejo
hasta que se quite y se vuelva a agregar.

## Estado
En la rama. **No publicado.**
