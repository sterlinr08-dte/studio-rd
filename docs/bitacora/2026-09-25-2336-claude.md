# Claude — Tienda: 360° de M12 rosa, XGW, HLD y las dos monta cargas (sin publicar)

## Contexto
- El dueño cambió el acceso a la red del entorno a **Completo**, y ahora se descarga directo de `res-prod.buzzy.now` sin que el dueño tenga que bajar los archivos.
- Nota: la pantalla dice que aplica a sesiones nuevas, pero en esta sesión funcionó de inmediato.

## Qué se hizo
- **Descargados los 6 videos** de Buzzy (Seedance 2.5 1080p 8 s; instrucción: plataforma invisible, fondo gris liso). Se procesaron con `giro2.py` (36 cuadros, rembg, marco común) y `limpia360.py` (borra los restos del círculo del piso).
- **Conectados en `CATALOGO`:**

  | Artículo | Giro |
  |---|---|
  | LQ-M12 | `giros: ['s700-rosa-v2', '', '']` (solo rosa por ahora; en blanco y gris se oculta el botón 360°) |
  | LQ-XGW | `lq-xgw-v2` |
  | LQ-HLD | `ilq-hld-v2` |
  | Monta carga hidráulico | `motocarga-hidraulica-v2` |
  | Monta carga 350 | `motocarga-350-v2` |

- **LQ-PP descartada:** el video iba y volvía en vez de dar la vuelta completa (la canasta nunca cambió de lado). No se conectó y se borró su carpeta. Se reenvió con una instrucción explícita de vuelta completa en un solo sentido.
- **Móvil, modo 360°:**
  - La pieza se veía pequeña: la zona de foto es baja y la vista frontal es alta y estrecha.
  - Ahora, mientras gira, se ocultan los datos, las características y el subtítulo de esa tarjeta, y el giro usa ese espacio: `clamp(200px, 100svh − 440px, 320px)`.
  - La tarjeta no crece y vuelve a la normalidad con «Ver foto».

## Pruebas (Chromium 390×664)
- Las 8 tarjetas con 360° (N9, H8, ZS, M12, XGW, HLD y las dos monta cargas) quedan completas dentro de la zona útil, en foto y girando.
- El visor carga y gira, y el botón 360° de la M12 se oculta en blanco y gris.
- Sin errores de página.

## Estado
Sin publicar. Rama `claude/fix-dock-breakpoint-860`.
