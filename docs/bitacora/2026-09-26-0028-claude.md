# Claude — URGENTE: la carpeta .git se servía en público y los despliegues fallaban desde la 59.45

## Hallazgo
- **Tienda en vivo congelada en 59.44.** Después de publicar la 59.50 la tienda seguía en 59.44. Los checks de GitHub «Workers Builds: studio-rd» dan **failure** en todos los commits de `main` desde `19ac249` (59.45) y **success** en `acc2b88` (59.44). Así que 59.45–59.50 nunca salieron en vivo.
- **Causa:**
  - `wrangler.jsonc` sirve `assets.directory: "./"`, es decir, todo el directorio clonado, **incluida la carpeta `.git`**, que `.assetsignore` no excluía.
  - **Seguridad:** `https://studiord.net/.git/config` y `/.git/HEAD` respondían 200, así que cualquiera podía reconstruir el repositorio y su historial.
  - **Despliegue:** con los videos y los cuadros 360°, el paquete de objetos de `.git` en el clon de Cloudflare supera el límite de 25 MiB por archivo de assets, y el build falla (explicación probable, coherente con el primer fallo al añadir `360/lq-h8-v2`).
- **NEXUS PRO:** `https://nexusprord.com/.git/config` también responde 200. Mismo problema; no se tocó sin autorización.

## Corrección (STUDIO)
- `.assetsignore` incluye `.git`, `.git/` y `.wrangler/`.
- Se publica junto con la 59.50, ya autorizada.

## Pendiente
- Verificar en vivo que `/.git/config` deja de devolver el archivo y que la versión sube a 59.50.
- NEXUS PRO: aplicar el mismo cambio con autorización del dueño.
- Revisar si en el historial del repositorio hay secretos (claves) y rotarlos, porque ya estuvieron expuestos.
