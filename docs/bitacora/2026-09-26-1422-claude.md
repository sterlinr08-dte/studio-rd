# Publicación 59.53 — carrito del catálogo mayorista

- Autorización del dueño: «Publicalo».
- Contenido: el commit d85563c, cuyos detalles y pruebas están en la bitácora anterior, más la subida de versión (`APP_VERSION` 59.53, `version.json` y AGENTS.md).
- Supabase no cambia. El enlace privado es el mismo.
- Verificación: el check de Workers Builds y que `studiord.net/version.json` y `/mayoristas` ya tengan el carrito.
